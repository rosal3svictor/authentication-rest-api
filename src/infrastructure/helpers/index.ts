import { APP_RESPONSE_LOG } from './AppResponseLog';
import { isEqualHelper } from './isEqualHelper';
import { isValidEmailHelper } from './isValidEmailHelper';

export const HELPER = {
  APP_RESPONSE_LOG,
  IS_EQUAL: isEqualHelper,
  IS_VALID_EMAIL: isValidEmailHelper,
};
